// JavaScript comes with a Math library with a number of useful tools for
// tweaking and generating numbers.

// Let's take a look at the following and try to predict these outputs
var floor = Math.floor(1.8);
var ceiling = Math.ceil( Math.PI );
var random = Math.random();

console.log(floor);
console.log(ceiling);
// in particular, can we predict what random will be?
console.log(random);

// Math.random() -- what range??
